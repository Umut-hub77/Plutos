# OPSİYONEL kanal: TradingView veri kaynağı.
# Bu paket PyPI'de yayınlanmıyor, sadece GitHub'dan kurulabiliyor ve resmi olarak
# bakımı yapılmıyor — bu yüzden ana requirements.txt'ten çıkarıldı.
#
# Kurulum önkoşulu: bilgisayarınızda Git kurulu ve PATH'te olmalı
#   Windows: https://git-scm.com/download/win adresinden kurup terminali yeniden açın
#   (kurulumdan sonra `git --version` çalışmalı)
#
# Kurmak isterseniz:
#   pip install -r requirements-optional.txt
#
# KURMASANIZ DA SORUN DEĞİL: data_engine.py, tvDatafeed kurulu değilse otomatik
# olarak Yahoo Finance (yfinance) kanalına düşer, uygulama normal çalışmaya devam eder.

tvDatafeed @ git+https://github.com/StreamAlpha/tvdatafeed.git
